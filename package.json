{
  "name": "baupilot",
  "version": "0.1.0",
  "private": true,
  "description": "AI-powered architecture assistant for German building rule checking",
  "scripts": {
    "build:packages": "pnpm --filter @baupilot/types build && pnpm --filter @baupilot/rule-engine build",
    "dev": "pnpm run build:packages && pnpm run --parallel dev",
    "dev:api": "pnpm --filter api dev",
    "dev:web": "pnpm --filter web dev",
    "build": "pnpm run build:packages && pnpm -r run build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "db:generate": "pnpm --filter api exec prisma generate",
    "db:migrate": "pnpm --filter api exec prisma migrate dev",
    "db:studio": "pnpm --filter api exec prisma studio"
  },
  "engines": {
    "node": ">=20"
  },
  "packageManager": "pnpm@9.14.2"
}
